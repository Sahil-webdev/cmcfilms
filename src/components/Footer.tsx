import { FooterOption1 } from "./footers/FooterOption1";

/**
 * Main Footer component wrapper.
 * Currently active: Footer Option 1 (CINEMATIC WINE PLUM EDITION).
 * Additional footer designs can be added to src/components/footers/ and switched here.
 */
export function Footer() {
  return <FooterOption1 />;
}
