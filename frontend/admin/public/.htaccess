<IfModule mod_rewrite.c>
  RewriteEngine On

  # Let the deployed admin assets load normally.
  RewriteCond %{REQUEST_FILENAME} -f [OR]
  RewriteCond %{REQUEST_FILENAME} -d
  RewriteRule ^ - [L]

  # Handle direct refreshes of routes within /admin.
  RewriteRule ^ index.html [L]
</IfModule>
