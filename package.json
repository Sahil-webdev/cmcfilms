{
  "name": "cmcfilms-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build:admin": "npm --prefix admin run build",
    "build:website": "npm --prefix website run build",
    "build": "npm run build:admin && npm run build:website"
  }
}
